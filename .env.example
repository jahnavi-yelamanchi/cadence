# Copy to .env and fill in values

# Weights & Biases (optional — training logs to W&B if set)
WANDB_API_KEY=

# Allow MPS ops that fall back to CPU on Apple Silicon
PYTORCH_ENABLE_MPS_FALLBACK=1

# Server config (override for production)
HOST=0.0.0.0
PORT=8000
